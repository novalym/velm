[project]
name = "elara-core"
version = "3.5.0-Ω"
description = "Extensible Logical Architectural Resonance Assembler (Standalone SGF)."
dependencies = [
    "pydantic>=2.6.0",
    "rich>=13.7.0",
    "networkx>=3.0",
    "psutil>=5.9.0"
]

[tool.poetry]
packages = [{include = "elara"}]